<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 磁贴 1 -->
        <service
            android:name=".TileService1"
            android:exported="true"
            android:icon="@drawable/ic_tile_default"
            android:label="Hermit Tile 1"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- 磁贴 2 -->
        <service
            android:name=".TileService2"
            android:exported="true"
            android:icon="@drawable/ic_tile_default"
            android:label="Hermit Tile 2"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- 磁贴 3 -->
        <service
            android:name=".TileService3"
            android:exported="true"
            android:icon="@drawable/ic_tile_default"
            android:label="Hermit Tile 3"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- 磁贴 4 -->
        <service
            android:name=".TileService4"
            android:exported="true"
            android:icon="@drawable/ic_tile_default"
            android:label="Hermit Tile 4"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- 磁贴 5 -->
        <service
            android:name=".TileService5"
            android:exported="true"
            android:icon="@drawable/ic_tile_default"
            android:label="Hermit Tile 5"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- 磁贴 6 -->
        <service
            android:name=".TileService6"
            android:exported="true"
            android:icon="@drawable/ic_tile_default"
            android:label="Hermit Tile 6"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

    </application>

</manifest>
